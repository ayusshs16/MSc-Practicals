# ==========================================
# SAMPLING
# ==========================================


# Q1. Create a population containing numbers from 1 to 100,
#     randomly select 10 observations, and calculate sample mean.

population <- 1:100
sampledata <- sample(population, 10)
samplemean <- mean(sampledata)
sampledata
samplemean


# Q2. Create a population from 1 to 50,
#     randomly select 5 observations, and calculate sample mean.

population <- 1:50
sampledata <- sample(population, 5)
samplemean <- mean(sampledata)
sampledata
samplemean


# Q4. Create a population from 1 to 200,
#     randomly select 20 observations, and calculate sample mean.

population <- 1:200
sampledata <- sample(population, 20)
samplemean <- mean(sampledata)
sampledata
samplemean


# Q5. Generate a random sample of 15 observations from 1 to 100
#     and calculate the sample mean.

population <- 1:100
sampledata <- sample(population, 15)
samplemean <- mean(sampledata)
sampledata
samplemean
